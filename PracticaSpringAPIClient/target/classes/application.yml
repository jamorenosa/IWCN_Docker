server:
  port: 8443
  ssl:
    key-store: classpath:keystore.jks
    key-password: password
spring:
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:mysql://localhost/pepesql
    username: pepe
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
